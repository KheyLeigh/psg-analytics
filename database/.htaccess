# Dossier interne : aucun accès direct par le web.
# L'application y accède par le système de fichiers via index.php.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
